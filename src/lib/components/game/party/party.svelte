<!-- <script lang="ts">
	import Text from '$lib/components/ui/text/text.svelte';
	import type { Unit } from '$lib/domain/unit/unit';
	import { getUnitById } from '$lib/states/game-state.svelte';

	let selectedUnitId: string = $state('');
	let selectedUnit: Unit = $derived.by(() => {
		return getUnitById(selectedUnitId);
	});
</script>

<div>
	<Text type="big">Party</Text>
</div> -->
